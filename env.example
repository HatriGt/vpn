# WireGuard Easy VPN Server Configuration
# Copy this file to .env and update with your values

# Your VPS public IP address or domain name
# Example: WG_HOST=123.45.67.89 or WG_HOST=vpn.yourdomain.com
WG_HOST=your-vps-ip-or-domain

# Password for the web UI (change this to a strong password!)
WG_PASSWORD=change-this-password

# WireGuard UDP port (default: 51820)
# Make sure this port is open in your firewall
WG_PORT=51820

# Web UI port (default: 51821)
# Access the web interface at http://your-vps-ip:51821
WG_WEB_PORT=51821

# Default VPN client IP range
# Format: 10.8.0.x (x will be assigned automatically)
WG_DEFAULT_ADDRESS=10.8.0.x

# DNS server for VPN clients
# Options: 1.1.1.1 (Cloudflare), 8.8.8.8 (Google), or your own DNS
WG_DEFAULT_DNS=1.1.1.1

# Allowed IPs for VPN clients
# 0.0.0.0/0, ::/0 = Route all traffic through VPN (full tunnel)
# Or specify specific IP ranges for split tunneling
WG_ALLOWED_IPS=0.0.0.0/0, ::/0

# MTU size (default: 1420)
# Adjust if you experience connection issues
WG_MTU=1420

# Persistent keepalive in seconds (default: 25)
# Helps maintain connection through NAT/firewalls
WG_PERSISTENT_KEEPALIVE=25
